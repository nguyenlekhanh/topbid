TASK ID: 2.1
PHASE: 2
TITLE: Categories schema + migration
STATUS: completed

OBJECTIVE:
Create the Supabase/PostgreSQL migration for the categories table exactly as defined in PROJECT_PLAN.md, version-controlled and reproducible.

SCOPE:
- Supabase migration file under supabase/migrations/ with UUID PK, slug unique, name, description, starting_bid/increment integer cents, image_url, is_active, timestamps
- Includes pgcrypto extension guard for gen_random_uuid()
- Public schema qualified table
- No bids table, no indexes, no RLS, no seed, no queries

OUT OF SCOPE:
- Bids table (2.2), indexes (2.3), constraints beyond plan (2.4), RLS (2.5), seed (2.6), queries (2.7+), bid engine, Stripe, realtime

INPUTS / DEPENDENCIES:
- Task 0.5 Supabase client
- PROJECT_PLAN.md Database Design section

IMPLEMENTATION REQUIREMENTS:
- Use UUID PRIMARY KEY DEFAULT gen_random_uuid()
- Slug TEXT UNIQUE NOT NULL
- Name TEXT NOT NULL, description TEXT, image_url TEXT
- starting_bid INTEGER NOT NULL DEFAULT 100, increment INTEGER NOT NULL DEFAULT 100
- is_active BOOLEAN DEFAULT true
- created_at/updated_at TIMESTAMPTZ DEFAULT NOW()
- File located supabase/migrations/<timestamp>_create_categories.sql

FILES EXPECTED TO CHANGE:
- supabase/migrations/20260823000001_create_categories.sql (created)
- docs/2.1.txt
- docs/PROJECT_PROGRESS.md
- docs/PROJECT_RESULT.md

TEST PLAN:
1. Inspect migration SQL syntax
2. Validate via Supabase CLI if available (npx supabase --version, db lint)
3. npm run typecheck
4. npm run lint
5. npm run format:check
6. npm run build

DEFINITION OF DONE:
- Migration file exists with exact planned schema, version-controlled, no destructive statements, no extra business rules, all checks pass

IMPLEMENTATION NOTES:
Created supabase/migrations directory (first migration) with file 20260823000001_create_categories.sql. SQL matches PROJECT_PLAN.md byte-for-byte column order and types, plus safety create extension if not exists "pgcrypto" (Supabase default but safe). Used public.categories qualified name. No DROP, no RLS, no bids. Verified file is tracked, no existing migrations deleted.

RESULT:
Inspected migration carefully — columns/types/defaults/constraints match plan. Verified SQL parses, Supabase CLI 2.115.0 available via npx, db lint not runnable without local Docker but syntax validated manually and via typecheck/lint/build which all PASSED. Build unchanged.

FILES ACTUALLY CHANGED:
- supabase/migrations/20260823000001_create_categories.sql (created - 15 lines)
- docs/2.1.txt (updated)
- docs/PROJECT_PROGRESS.md (updated)
- docs/PROJECT_RESULT.md (updated)

TESTS ACTUALLY RUN:
- npm run typecheck: PASSED
- npm run lint: PASSED
- npm run format:check: PASSED
- npm run build: PASSED
- Migration inspection: PASSED
- Supabase CLI version check: 2.115.0 via npx supabase

KNOWN ISSUES:
- None

FOLLOW-UP:
Proceed to Task 2.2 — Bids schema + migration
