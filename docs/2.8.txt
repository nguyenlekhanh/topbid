TASK ID: 2.8
PHASE: 2
TITLE: Highest bid query
STATUS: completed

OBJECTIVE:
Create a reusable server-side Supabase query to get the highest paid bid for a given category, respecting RLS and existing conventions.

SCOPE:
- New file src/lib/bids.ts with Bid type and getHighestBidForCategory(categoryId)
- Uses server client (supabase-server anon, no service_role), respects RLS status='paid' only
- Ordered amount DESC limit 1, returns Bid | null when none, handles invalid id as null, throws predictably on other errors

OUT OF SCOPE:
- Leaderboard (2.9), recent bids (2.10), bid engine/Stripe/realtime/auth, UI redesign, migrations/RLS changes

INPUTS / DEPENDENCIES:
- Tasks 2.1-2.7 (categories/bids schema, indexes idx_bids_category_paid_amount, RLS bids_public_select_paid, seed, category queries)
- src/lib/supabase-server.ts pattern from categories.ts

IMPLEMENTATION REQUIREMENTS:
- Server-side only, anon key, no service-role import, preserve RLS (only paid)
- Status = 'paid' filter, amount DESC, limit 1, null when none, typed Bid

FILES EXPECTED TO CHANGE:
- src/lib/bids.ts (created)
- docs/2.8.txt
- docs/PROJECT_PROGRESS.md
- docs/PROJECT_RESULT.md

TEST PLAN:
1. npm run typecheck
2. npm run lint
3. npm run format:check
4. npm run build
5. Inspect RLS/server-client usage, order/limit logic, not-found handling

DEFINITION OF DONE:
- Typed query exists, RLS-compliant, server-side, null when none, all checks pass

IMPLEMENTATION NOTES:
Inspected categories.ts pattern (async createClient, CATEGORY_FIELDS, is_active filter, maybeSingle). Created bids.ts mirroring conventions: Bid type with 11 fields matching bids migration, BID_FIELDS allowlist, getHighestBidForCategory validates categoryId string trim, uses await createClient() (anon, not service_role), query .from('bids').select(BID_FIELDS).eq('category_id', normalized).eq('status','paid').order('amount',{ascending:false}).limit(1).maybeSingle(). Throws on error with message, returns null on no rows. Preserves RLS and uses idx_bids_category_paid_amount.

RESULT:
Verified via typecheck/lint/format:check/build all PASSED. Inspection confirmed server client, RLS paid-only, DESC limit 1, null handling, no service_role, reusable lib.

FILES ACTUALLY CHANGED:
- src/lib/bids.ts (created - 48 lines)
- docs/2.8.txt (updated)
- docs/PROJECT_PROGRESS.md (updated)
- docs/PROJECT_RESULT.md (updated)

TESTS ACTUALLY RUN:
- npm run typecheck: PASSED
- npm run lint: PASSED
- npm run format:check: PASSED
- npm run build: PASSED
- Code inspection: PASSED (server/client, RLS, order/limit, null)

KNOWN ISSUES:
- None

FOLLOW-UP:
Proceed to Task 2.9 — Leaderboard query
