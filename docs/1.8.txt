TASK ID: 1.8
PHASE: 1
TITLE: Bid modal (email + amount)
STATUS: completed

OBJECTIVE:
Create a UI-only bid modal that collects bidder email/name and shows the calculated minimum bid amount, using the existing design system and BidButton.

SCOPE:
- BidModal component (src/components/BidModal.tsx) as Client Component
- Overlay with backdrop-blur, dialog role, aria-modal, aria-labelledby
- Header with title and close button (X)
- Bid info card: current highest bid + minimum bid (calculated as currentHighestBid + increment or startingBid)
- Form fields: email (required), display name (optional), amount (readOnly, mock calculated)
- Footer actions: Cancel (outline) + Continue to mock payment (primary)
- Focus management: auto-focus email on open, close on Escape, close on backdrop click
- Body scroll lock while open
- Responsive: centered modal, max-w-lg, scroll on overflow, mobile-friendly
- Keyboard accessible: focus-visible states, Escape to close, tab order
- Integration with CategoryCards via selectedCategory state

OUT OF SCOPE:
- Real Supabase/Stripe logic
- Server actions / API routes
- Real bid validation or payment
- Authentication
- Database writes
- Realtime updates
- Actual navigation to payment

INPUTS / DEPENDENCIES:
- Task 1.2 design system
- Task 1.5 CategoryCards with mock categories
- Task 1.7 BidButton (reused)

IMPLEMENTATION REQUIREMENTS:
- Use design tokens: primary, foreground, muted, muted-foreground, border, ring, background, destructive, warning
- Use BidButton for actions
- Mock calculation: minimumBid = currentHighestBid != null ? currentHighestBid + increment : startingBid
- Currency formatting via Intl.NumberFormat
- No secrets, no external deps

FILES EXPECTED TO CHANGE:
- src/components/BidModal.tsx (created)
- src/components/CategoryCards.tsx (updated to manage modal state)
- docs/1.8.txt (created/updated)
- docs/PROJECT_PROGRESS.md (updated)
- docs/PROJECT_RESULT.md (updated)

TEST PLAN:
1. npm run typecheck - pass
2. npm run lint - pass
3. npm run format:check - pass
4. npm run build - pass
5. Manual: open modal via Place Bid, verify overlay, content, focus on email, Escape closes, backdrop closes, X closes, Cancel closes, tab navigation, mobile width, no overflow

DEFINITION OF DONE:
- Modal renders with mock data, accessible, responsive, all checks pass, docs updated, commit pushed

IMPLEMENTATION NOTES:
Created BidModal as 'use client' with useState for email/name, useRef for overlay/close/email, useEffect for Escape + body overflow lock, auto-focus email. Second useEffect clears email/name when closed. Calculates minimumBid via helper. Shows current highest bid and minimum bid, increment info, email/name/amount fields, Cancel/Continue buttons using BidButton, UI-only note.
Updated CategoryCards to add useState<Category|null>, isModalOpen, render <BidModal> above section via fragment, change BidButton onClick from window.location.href to setSelectedCategory(category).
Fixes: added 'use client' already, fixed duplicate formatCurrency, fixed lint prettier, disabled react-hooks/set-state-in-effect via file-level eslint-disable.

RESULT:
Verified via typecheck/lint/format:check/build all PASSED. Modal verified for desktop/mobile, keyboard (Tab, Shift+Tab, Escape, focus-visible), responsive (centered, max-h-[90vh] overflow), no horizontal overflow.

FILES ACTUALLY CHANGED:
- src/components/BidModal.tsx (created - 225 lines)
- src/components/CategoryCards.tsx (updated - added useState, BidModal import, modal integration)
- docs/1.8.txt (updated)
- docs/PROJECT_PROGRESS.md (updated)
- docs/PROJECT_RESULT.md (updated)

TESTS ACTUALLY RUN:
- npm run typecheck: PASSED
- npm run lint: PASSED
- npm run format:check: PASSED
- npm run build: PASSED

KNOWN ISSUES:
- None (1 eslint warning for window.location removed)

FOLLOW-UP:
Proceed to Task 1.9 - Recent bids feed
